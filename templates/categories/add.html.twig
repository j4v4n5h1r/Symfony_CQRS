<div class="row">
    <div class="offset-3 col-6">
        <div class="card">
            <div class="card-body">
                {{ form_start(form,{'attr':{'class':'form-material m-t-40'}}) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>