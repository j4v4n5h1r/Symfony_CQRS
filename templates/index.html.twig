<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
	<h1>hello world!</h1>

	<h3>new post</h3>
	<form action="post" method="get">
		Post: <input type="text" name="name">
		Category: <input type="text" name="category">
		<button type="submit">Add</button>
	</form>
	<h3>posts: </h3>
	<hr>
	<hr>
	{% for post in posts %}
	<a href="post/{{ post.id }}">{{ post.name }}</a>
	<hr>
    {% endfor %}
    <hr>

    <a href="/user">Users page (login, register)</a>
</body>
</html>